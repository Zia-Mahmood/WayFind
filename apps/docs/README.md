# Wayfind docs site

The documentation site plus the marketing asset: a hosted, interactive live demo map of a well-known OSS repo (e.g. Express) with a recorded trace — "watch an HTTP request travel through the system" without installing anything.

**Status:** not yet implemented — built in [M6](../../docs/ROADMAP.md). Until then, docs live as markdown in [docs/](../../docs/).
