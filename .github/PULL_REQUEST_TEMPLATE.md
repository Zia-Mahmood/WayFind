## What & why

<!-- Link the issue. One paragraph: what changes and why. -->

## How I verified it

<!-- Commands run, output observed. "Tests pass" needs the test names. -->

## Checklist

- [ ] Conventional Commit title (e.g. `feat(renderer): ...`)
- [ ] Tests added/updated; golden snapshots in `examples/` updated if adapter output changed (explain diffs below)
- [ ] Changeset added (`pnpm changeset`) if user-visible
- [ ] Format change? → version bump + migration notes + ADR
- [ ] No telemetry / network calls introduced
