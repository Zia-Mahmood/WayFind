# @wayfind/playback

Loads `.wayfind-trace` bundles and turns them into a scrubbable timeline: state reconstruction at any time *t* (time travel), value-change events for edge/node rendering, exception propagation paths for the rupture view, and index/trace version-mismatch detection.

**Status:** not yet implemented — lands in [M4](../../docs/ROADMAP.md).
